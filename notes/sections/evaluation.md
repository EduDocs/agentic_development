---
videos:
  - title: Evaluation Harnesses
    url: https://youtu.be/AFDTbhK2PFE
---

# evaluation — scratch

> Sidecar for `evaluation.tex`. Not part of the LaTeX build.
> The EVOLVE-BLOCK below is the shareable spine exported to reviewers and elites;
> everything outside it is private scratch you may freely edit.

## Purpose
Build the harness that scores an artifact honestly — the object that becomes the
fitness function in Ch.9. Establish leakage-resistance and eval-hacking defenses.

<!-- EVOLVE-BLOCK-START -->
## Key points / spine
- **Harness = $(\mathcal{D}, \Score)$** with a sealed train/test partition (Def.~EvalHarness); the test split is inviolable (Principle~TestSplitInviolable).
- Automatic evaluation earns three properties the evolutionary half requires: speed, consistency, composability. A good score is faithful, discriminating, leakage-resistant, stable.
- **Leakage** taxonomy (scoring on train; feature construction from the full set; adaptive re-splitting) and the **closure-based leakage-resistant design** (the scorer is a closure over sealed test data).
- Scoring: MSE, **parsimony/complexity**, composite $\Score = \mathrm{MSE} + \lambda C$ (lower is better). **Eval hacking / Goodhart**: any metric pointed at by a persistent optimizer is attacked.
- **Harness as a contract** `(score, report)` — built once, called by every later unit, and reused unchanged as the Ch.~EvolutionaryFrameworks fitness function. This is the mechanical realization of Spine 1.
- **Benchmark saturation + eval awareness** (temporal Goodhart): a metric saturates as capability rises, so grade the *outcome* and *graduate* the task; and a tool-using agent can retrieve its own answer key, so leakage-resistance must sandbox tools, not just partition data. The course's numeric harness is one instance of the field's agent-capability benchmarks (SWE-bench, WebArena, GAIA, OSWorld).

## Directions
- (no open prose directions for this chapter; the rigor/novelty/referee items flagged at baseline are realized — see Decisions)

## Decisions
- 2026-07-10: sign convention "lower is better" fixed in this chapter and carried through elitism (Ch.9) — rigor panel confirmed the manuscript is internally consistent on the convention, so do not disturb it.
- 2026-07-10 (realized in prose): complexity now defined once as node count (with the $f_1{=}3$/$f_2{=}9$ counting shown); the "trivial re-parameterization" exploit restated correctly as node count undercounting fitted-constant degrees of freedom (the false "$c$ and $2x$ are the same model" claim is gone).
- 2026-07-10 (realized in prose, currency): added a **§LLM-as-Judge Evaluation** section — a capable model scoring open-ended outputs against a rubric is the same harness contract with a model-call scorer (Zheng et al., 2023); names the calibration risk (the judge's biases become the harness's biases) and invokes Principle~GoodhartsLaw (a weak judge is exploited like a weak metric). Added a **scope note** in §Evaluation Principle: the MSE+parsimony harness suits the SR vehicle; qualitative tasks need LLM-as-judge or human eval. Prevents over-generalizing the numerical harness.
- 2026-07-10 (realized in prose, visual): added the **leakage figure** (`figure:Leakage`) at §What Leakage Is — split-first data flow (dataset → train/test; train → agent → candidate → score; sealed test used exactly once, in scoring) with the forbidden dashed-red test→agent arrow crossed out; the caption maps all three example leakage forms onto that one arrow. Matches the proposer–critic diagram's visual language (box/arr styles, same fills).
- 2026-07-10 (realized in prose, clarity): **"model" overloading anchored** at Def.~EvalHarness — a terminology note fixes $a \in \Artifacts$ as the *candidate artifact / candidate expression*, reserving "model" for the language model $M$ (Ch.3) and flagging the "model of the data" reading explicitly. Panel's top clarity defect; Ch.7–9 should use "candidate expression" wherever the symbolic expression is meant.
- 2026-07-10 (realized in prose): **Goodhart credited** at Principle~GoodhartsLaw — an attribution sentence names Charles Goodhart (1975, the policy-target collapse) and Strathern (1997, the compact "measure under optimization" formulation). **Sculley citation repaired** — the mis-titled "Challenging Common Assumptions..." reference is replaced by Sculley, Snoek, Wiltschko & Rahimi, "Winner's Curse? On Pace, Progress, and Empirical Rigor" (ICLR Workshop 2018), a correctly-titled, on-topic call for evaluation discipline.
- 2026-07-11 (realized in prose, engineering-blog pass): extended leakage to **tool-using agents** that retrieve their own answer key (eval awareness, 2026); added a **temporal-Goodhart / benchmark-saturation** Remark (grade the outcome, graduate the task) closing the Ch.1 fitness-decay forward-ref; named **SWE-bench / WebArena** as the field's agent-capability benchmarks, framing the course harness as one instance (closes a currency gap); Further Reading on demystifying-evals, eval-awareness, AI-resistant-evals, SWE-bench.
- 2026-07-11 (realized in prose, practitioner-thread pass): added an **evaluator-portability** Remark after the harness contract — fitness evaluation ranges from portable (any sandbox) to infrastructure-bound (compiled simulators, GPU, licensed tools, long wall-clock); deciding what the signal requires, then pinning (Ch.4) and sandboxing (Ch.9) it, is part of harness design. The least-portable signal is often the most informative — the part a general platform cannot supply.
- 2026-07-12 (external REVIEW.md pass — **Priority 0, train/validation/test**): the review's central correction. The reused, repeatedly-queried scorer that becomes fitness is a **validation/fitness** signal, not a held-out **test** set. Reworked the harness Definition to a three-way split (train fits / validation-fitness selects / final-test audits, one-shot); made the three roles explicit and added the **adaptive-overfitting** point (a sequence of validation scores erodes the split even with raw rows hidden); renamed the closure `score`→`fitness_score` over `X_val`/`y_val` and demoted it from a "security boundary" to a convenience; fixed the figure caption's "used exactly once" contradiction; qualified "deterministic"→"repeatable under controlled conditions"; set the sharpened slogan **"a reusable validation harness becomes fitness; a final-test harness remains an audit."** Propagated the terminology to Ch.2 (`principle:EvalFitness` no longer claims *every* harness drops in unmodified), Ch.5 (`fit_model` returns validation error), Ch.6 commit-message example, and Ch.9 (SR argmin over $\mathcal{D}_{\mathrm{val}}$; sandbox/leakage wording). Deferred (documented): Priority 1 (a `\cite`/biblatex overhaul conflicts with the hand-written-citation convention + metrics gate) and Priority 2 (course-apparatus buildout — large/structural, better attended).
- 2026-07-14 (panel attempt-17 fixes): brought **Eq.~MSE / §Predictive Error** into line with the three-way-split discipline — the MSE definition now names *the evaluation (validation) split*, not "test data" (the harness scores per candidate on $\mathcal{D}_{\mathrm{val}}$; the sealed test is read once), and repaired the adjacent loose "test data" in §Parsimony. A consistency fix realizing the already-settled Priority-0 framing, not a new claim. Separately, **redesigned `figure:EvaluabilitySpectrum` to be relational** (visual reviewer): one Candidate branches to two scoring mechanisms — an Oracle (formula/test) and a Critic (model + rubric), each yielding a score — with a dashed "no formula" arrow showing the top lane collapse into the bottom where no oracle exists; mechanism is now structural, not a decorated word-pair. Already sourced `concept-style.tex`.
- 2026-07-14 (panel attempt-18, currency): extended the capability-benchmark sentence to name **GAIA** (general assistant) and **OSWorld** (computer use) alongside SWE-bench and WebArena, framing the course harness as one instance of a broader, named evaluation culture. Light touch; the three-role validation/fitness/test gloss the clarity reviewer asked for was judged already present in the post-Definition prose, so not duplicated.
- 2026-08-06 (de-course-ification, repo-wide): the notes now stand alone from the course's unit apparatus — every `Challenge~N` reference is replaced by a chapter cross-reference or course-neutral phrasing. Here: the harness contract is no longer scoped to "Challenges 2–4" but to "built once and then called by every later unit of the course", and the one-shot audit is of "the retained solution" rather than "the winner". Spine bullet updated to match. The same pass ran across Ch.~Retrieval, Ch.~Orchestration, and Ch.~EvolutionaryFrameworks — see their sidecars.
- 2026-08-06 (realized in prose, framing): the one-sentence **Goodhart attribution expanded into a worked account** of the 1975 monetary-aggregate case — a stable aggregate/income relation, adopted as a policy target, banks reorganize around the measured category and move lending outside it, the relation breaks. It lands on a general mechanism the bare adage never stated: an observed relation may genuinely capture a phenomenon, but once the marker becomes the objective the optimization acts on the marker alone and is free to move it by any route — **"the correlation was never load-bearing; targeting is what puts load on it."** This gives Principle~GoodhartsLaw a causal story instead of an appeal to authority; Strathern (1997) still supplies the compact formulation.

<!-- EVOLVE-BLOCK-END -->

## Open questions
- Should $\lambda$ calibration guidance live here or in Ch.9's Fitness Design? Currently here (Remark); Ch.9 should mirror the *same* treatment for its cost knob $\alpha$.
