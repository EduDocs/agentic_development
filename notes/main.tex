%% main.tex: what is specific to *this* book.
%%
%% The machinery (packages, fonts, theorem environments, listings style) lives
%% in templates/preamble.tex. What stays here is the class, this book's
%% notation, its identity, and its structure: the things you would have to
%% rewrite to make a different book.

\documentclass[12pt,openany]{book}

\input{templates/preamble}

%%  Notation
% This book's symbols, used across sections/. Machinery lives in the preamble;
% these are content, so they stay where an author will look for them.
\newcommand{\Artifacts}{\mathcal{A}}
\newcommand{\Population}{\mathcal{P}}
\newcommand{\Fitness}{f}
\newcommand{\Mutate}{\mu}
\newcommand{\Select}{\sigma}
\newcommand{\RealNumbers}{\mathbb{R}}
\newcommand{\NaturalNumbers}{\mathbb{N}}
\newcommand{\Score}{\mathrm{score}}
\newcommand{\Eval}{\mathrm{eval}}
\newcommand{\Loss}{\mathcal{L}}

%%  Identity
\renewcommand{\today}{Fall 2026}

% PDF document properties, distinct from the title page: these are what a
% browser tab, a reader's Properties panel, and an indexer report. pdflang is
% the accessibility-relevant one, since without it a screen reader has to guess
% the pronunciation rules.
% pdfauthor names the organization rather than a person, so that the credit
% stays correct as the notes accumulate contributors; the title page carries no
% author line at all, which is the same choice made twice.
\hypersetup{
  pdftitle={Agentic Development and Discovery: Empowering Researchers in Compute-Adjacent Fields},
  pdfauthor={EduDocs (github.com/EduDocs)},
  pdfsubject={Course notes on agentic development and agentic evolutionary frameworks for research},
  pdfkeywords={agentic development, evolutionary frameworks, symbolic regression,
               evaluation harness, context engineering, retrieval, orchestration,
               research methods, graduate course},
  pdflang={en},
}

\begin{document}

\input{templates/header}

\author{}
\title{
\Huge{Agentic Development \& Discovery}\\[5mm]
\Large{Empowering Researchers in Compute-Adjacent Fields}\\[4mm]
\large{Course Notes}}

\frontmatter
\maketitle

\setcounter{tocdepth}{1} % chapters and sections only; suppress subsections
\tableofcontents

% Each \include names the SHIPPED register of a section pair — the extension is
% omitted, so sections/<name>.prose resolves to sections/<name>.prose.tex. Its
% sidecar is sections/<name>.concepts.md, never included (ADR 0015, ADR 0019).
\include{sections/preface.prose}

\mainmatter

\include{sections/how_we_got_here.prose}
\include{sections/agentic_paradigm.prose}
\include{sections/agentic_rig.prose}
\include{sections/context_engineering.prose}
\include{sections/agentic_coding.prose}
\include{sections/agent_loop.prose}
\include{sections/evaluation.prose}
\include{sections/retrieval.prose}
\include{sections/orchestration.prose}
\include{sections/evolutionary_frameworks.prose}

\appendix
\include{sections/rig_reference.prose}
\include{sections/cowork_reference.prose}

\printindex

\input{templates/footer}

\end{document}
